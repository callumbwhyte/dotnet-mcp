# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: dotnet-mcp
services:  
  app:
    language: dotnet
    project: ./src/MCP.Aspire.AppHost/MCP.Aspire.AppHost.csproj
    host: containerapp
